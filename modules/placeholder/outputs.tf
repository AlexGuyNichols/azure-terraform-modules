# Placeholder module has no resources and therefore no outputs.
# This file exists so the CI matrix can exercise the complete tool suite
# (fmt, validate, tflint, checkov) against a structurally valid module.
# Deleted in Phase 2 when the key-vault module lands.
